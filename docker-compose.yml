
services:
  database:
    image: postgres:18-alpine
    shm_size: 128mb
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=speakeasy
      - POSTGRES_USER=speakeasy
      - POSTGRES_PASSWORD=speakeasy
    volumes:
      - ./_volumes/postgres:/var/lib/postgresql/18/docker
    